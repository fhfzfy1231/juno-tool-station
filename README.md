# Juno Tool Station

面向《朱诺：新起源》（Juno: New Origins）的源码公开网页工具站，站点地址为 [tool.juno.foxbridge.team](https://tool.juno.foxbridge.team)。项目使用 React 19、TypeScript、Vite 与 Vinext 构建。

## 项目目标

Juno Tool Station 希望把游戏中常用但分散的计算、设计与文件处理功能集中到同一个工作台中：

- 提供无需安装、打开浏览器即可使用的 Juno 专用工具；
- 优先在本地浏览器处理图片和飞船文件，不上传用户数据；
- 以游戏实际支持能力为准，输出可直接复制或导入游戏的结果；
- 让飞船文件、发动机参数、Δv 结果与轨道规划数据可以在不同工具间继续使用；
- 保持桌面端与移动端界面一致，并通过模块化结构持续扩展工具。

## 当前进度

### 已完成

- [x] 工具站首页、响应式导航与开发路线展示；
- [x] 首页工具卡片和路线阶段说明弹窗；
- [x] 接入独立教程站 [juno.foxbridge.team](https://juno.foxbridge.team)；
- [x] 图片转 Label 2.2：图片导入、实时预览、代码复制与分片导出；
- [x] 使用游戏可正常读取的 2–10 色调色板输出；
- [x] 提供纯色块、Bayer、Atkinson 与 Floyd 四种量化/抖动算法；
- [x] 支持 1–4 列、1–4 行分片，并显示最大分片与当前选中分片的负载评级；
- [x] 支持预览缩放、拖动，以及悬停查看分片编号；
- [x] 图片转换完全在本地浏览器中完成。

### 计划中

- [ ] 飞船 XML 文件解析与零件结构读取；
- [ ] 多级火箭质量、推重比和 Δv 计算；
- [ ] 三维轨道、机动节点与任务窗口规划；
- [ ] MFD 页面设计、图层管理与模板导出；
- [ ] 搜索引擎收录推送管理模块。

## 本地运行

要求 Node.js 22.13 或更高版本。

```bash
npm install
npm run dev
```

开发服务器默认运行在终端显示的本地地址。

## 构建与运行

```bash
npm run build
npm run start
```

## 1Panel 部署

当前版本推荐通过 1Panel 的「Node.js 运行环境」部署：

1. 将整个项目上传到服务器，例如 `/opt/1panel/apps/juno-tool-station`。
2. 在 1Panel 应用商店安装 Node.js 运行环境，Node 版本选择 22。
3. 创建 Node.js 网站，工作目录选择项目目录。
4. 安装命令填写 `npm ci`。
5. 构建命令填写 `npm run build`。
6. 启动命令填写 `npm run start`。
7. 根据运行日志确认应用端口，在网站配置中反向代理到该端口。
8. 为网站绑定你准备使用的域名。
9. 申请并启用 HTTPS 证书，开启 HTTP 自动跳转 HTTPS。

## 搜索引擎推送模块规划

后续增加独立管理页面 `/admin/search-push`，但 API 密钥不能保存在浏览器 LocalStorage 或前端源码中。

```text
管理界面
  └─ 调用受保护的服务端 API
       ├─ 百度普通收录推送
       ├─ 必应 URL Submission API
       ├─ 手动推送指定 URL
       ├─ sitemap 批量推送
       └─ 定时扫描并自动推送新增/更新页面
```

服务端需要记录：

- 搜索引擎 API Token；
- 最近一次推送时间；
- URL、内容更新时间和推送状态；
- 每次请求的结果和错误信息；
- 每日配额使用情况。

自动推送由服务器定时任务执行，不能依赖用户打开网页。1Panel 中可使用计划任务调用内部推送接口，或由 Node 服务自身使用任务调度器执行。

安全要求：

- 管理界面必须登录；
- Token 只在服务端保存；
- 前端接口不能返回完整 Token；
- 推送接口需要防止未授权调用和重复提交；
- 日志不得输出 Token。

## 开源协议

本项目采用 [PolyForm Noncommercial License 1.0.0](./LICENSE)。允许个人学习、研究、娱乐、非营利组织及其他非商业用途使用、修改和分发源码，但禁止未经授权的商业使用。

如需将本项目或其修改版本用于付费产品、商业服务、广告获利或其他商业用途，请先取得作者的单独授权。

项目中的游戏名称、商标以及用户自行导入的图片或游戏资源不因本协议获得额外授权。
